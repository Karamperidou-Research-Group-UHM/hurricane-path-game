events {}
http {
    # Handles routing.
    server {
        # Initial root.
        root /usr/share/nginx/html/;

        # If the location ends with just a slash, redirect to /startup/index.html.
        location = / {
            index /startup/index.html;
        }
    }
}
